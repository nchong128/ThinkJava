/** 
    && for AND
    || for OR
    ! for NOT

    - Logical operators evaluate the second expression only if needed
    - de Morgan's Laws
        - !(A && B) === !A || !B
        - !(A || B) === !A && !B

 */